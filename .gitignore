# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# macOS
.DS_Store

# local review logs (never committed)
.codex-review.log
.codex*
.deepseek*
docs/dev-review/

# llmeter runtime output (never belongs in the repo)
usage-snapshot.json
usage-history.jsonl
*.llmeter-bak-*
*.llmeter-tmp

# git worktrees live in the project, never committed
worktrees/

# local build/test logs (never committed)
build_logs/
